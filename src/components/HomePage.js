import React from 'react';

// styles
import styles from "./HomePage.module.css";

const HomePage = () => {
    return (
        <div>
            home page
        </div>
    );
};

export default HomePage;